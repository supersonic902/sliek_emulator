<canvas>
  z16o   //this is the bit processer
    var.control(up) = input.key.(w), var.control(down) = input.key.(s), var.control(left) = input.key.(a), var.control(right) = input.key.(d)
  if  var.control(up) = true
    then int <sprite.control.trigger(up)>
  if <control> = down
    then int <control.trigger(down)>
